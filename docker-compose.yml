version: '3.8'

services:
  web:
    build: .
    ports:
      - "7777:7777"
    env_file:
      - .env
    volumes:
      - uploads:/app/app/uploads
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mywealthpilot
      POSTGRES_USER: mywealthpilot
      POSTGRES_PASSWORD: ${DB_PASSWORD:-mywealthpilot_secret}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
  uploads:
